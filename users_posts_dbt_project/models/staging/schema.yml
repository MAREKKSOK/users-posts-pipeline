version: 2

models:
  - name: stg_posts
    columns:
      - name: post_id
        tests:
          - not_null
          - unique
      - name: user_id
        tests:
          - not_null
          - relationships:
              to: ref('stg_users')
              field: user_id
  - name: stg_users
    columns:
      - name: user_id
        tests:
          - not_null
          - unique
      - name: username
        tests:
          - not_null
        